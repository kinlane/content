---
layout: none
---
[
	{% for post in site.posts limit:25 %}{
    "title": "{{ post.title }}",
    "url": "http://apievangelist.com{{ post.url }}",
    "updated": "{{ post.atomdate | date: "%Y-%m-%dT%H:%M:%SZ" }}",
    "image": "{{ post.image }}",
    "github_user": "{{ post.github_user }}",
    "github_repo": "{{ post.github_repo }}"
  }{% if forloop.last %}{% else %},{% endif %}{% endfor %}
]
